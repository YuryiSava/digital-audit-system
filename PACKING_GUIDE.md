# 📦 Инструкция по упаковке и переносу системы

Чтобы перенести систему на новый ПК максимально быстро и без лишнего веса, следуйте этой инструкции.

---

## 🛠 Шаг 1: Подготовка (Очистка)
Перед тем как делать архив, удалите эти папки (они весят ~800 МБ и не нужны для переноса, скрипт сам их создаст):
1.  **`node_modules`** — (ОЧЕНЬ ВАЖНО УДАЛИТЬ)
2.  **`.next`** — (Временная папка сборки)
3.  **`dist`** или **`build`** (если есть)

## 📦 Шаг 2: Упаковка
Выделите все оставшиеся файлы и папки и добавьте их в **ZIP-архив**. 
**Убедитесь, что файл `.env` попал в архив!** (В Windows он скрыт, проверьте настройки вида).

### Что ОБЯЗАТЕЛЬНО должно быть в архиве:
- `app/`, `components/`, `public/`, `utils/` и др. папки кода.
- `package.json` и `package-lock.json` (инструкции для установки).
- `.env` (Ключи доступа к базе и ИИ).
- **`INSTALL-AND-RUN.bat`** (Наш новый скрипт запуска).

---

## 🚀 Шаг 3: Запуск на новом месте
1.  Распакуйте архив на новом компьютере в любую папку.
2.  Убедитесь, что установлен **Node.js** (скачайте с nodejs.org, если нет).
3.  Просто запустите файл **`INSTALL-AND-RUN.bat`** двойным кликом.

Скрипт сам скачает недостающие части и запустит программу. Вам останется только открыть браузер по адресу `http://localhost:3000`.

---
**Ваш архив теперь весит всего около 5-10 МБ (вместо 1 ГБ), и его легко отправить даже по почте!** ✈️📦
