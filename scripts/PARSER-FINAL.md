# External PDF Parser - Финальная Версия

## ✅ Реализовано

### 1. External GPT Parser
- Надежный парсер через GPT-4o-mini API
- Автоматическое извлечение требований из PDF
- Приоритет русского текста в билингвальных документах
- Автоматическая генерация тегов
- Стандартизация systemId для предотвращения ошибок FK

### 2. UI Интеграция
- ✅ Кнопка "Запустить AI Парсинг" на странице норматива
- ✅ Выбор целевой системы (APS/SOUE/CCTV и т.д.)
- ✅ Показ прогресса парсинга
- ✅ Автоматическое обновление страницы после завершения

### 3. Управление Требованиями
- ✅ Кнопка "Добавить требование" - ручное добавление
- ✅ Кнопка "Удалить" на каждом требовании
- ✅ Отображение тегов в UI
- ✅ Все поля: система, пункт, описание, метод проверки, теги

### 4. Защита от Потери Данных ⚠️
- ✅ Проверка существующих требований перед парсингом
- ✅ Предупреждение если есть вручную добавленные требования
- ✅ Требуется подтверждение при перезаписи
- ✅ Детальное логирование (сколько требований будет удалено)
- ❌ Пакетная обработка **ОТКЛЮЧЕНА** в UI (опасно для данных)

## 📋 Файлы

### Server Actions:
- `app/actions/external-parser.ts` - вызов external скриптов
  - `parseNormWithExternalGPT()` - парсинг одного норматива
  - `checkExistingRequirements()` - проверка существующих требований
- `app/actions/requirements.ts` - CRUD операции с требованиями
  - `addRequirement()` - добавление вручную
  - `deleteRequirement()` - удаление

### UI Components:
- `app/norm-library/[id]/parse-button.tsx` - кнопка парсинга с защитой
- `app/norm-library/[id]/add-requirement-button.tsx` - добавление вручную
- `app/norm-library/[id]/delete-requirement-button.tsx` - удаление
- `app/norm-library/[id]/page.tsx` - страница норматива (revalidate = 0)

### Scripts (standalone):
- `scripts/parse-pdf-with-gpt.js` - основной парсер
- `scripts/parse-all-norms.js` - пакетная обработка (только вручную!)
- `scripts/README-PARSER.md` - документация
- `scripts/QUICKSTART.md` - быстрый старт

## 🚀 Использование

### Через UI (рекомендуется):
1. Откройте норматив
2. Нажмите "Запустить AI Парсинг"
3. Выберите систему
4. Подтвердите если есть предупреждение
5. Дождитесь завершения (1-3 минуты)

### Через командную строку:
```bash
# Парсинг одного норматива
node scripts/parse-pdf-with-gpt.js <norm-id>

# С указанием системы
node scripts/parse-pdf-with-gpt.js <norm-id> APS

# Пакетная обработка (ОСТОРОЖНО!)
node scripts/parse-all-norms.js
```

## ⚠️ Важные Предупреждения

### Пакетная обработка
- **НЕ используйте** `parse-all-norms.js` если в базе есть вручную добавленные требования
- Скрипт удаляет ВСЕ существующие требования перед вставкой новых
- Используйте только для первичного наполнения пустой базы
- Кнопка убрана из UI для предотвращения случайной потери данных

### Повторный парсинг
- При повторном парсинге через UI появится предупреждение
- Система покажет сколько требований будет удалено
- Особое предупреждение для вручную добавленных требований
- Требуется явное подтверждение

## 📊 Статистика

### Протестировано на:
- СН РК 2.02-01-2023 - 10 требований извлечено
- Билингвальный документ (казахский/русский)
- 153,126 символов текста
- Время парсинга: ~2-3 минуты
- Стоимость: ~$0.01 за документ

### Технические детали:
- Модель: GPT-4o-mini
- Timeout: 5 минут на документ
- Max buffer: 10MB
- Validation: systemId, UUID generation
- Caching: отключен (revalidate = 0)

## 🔧 Настройка

### Environment Variables:
```env
# .env.local
OPENAI_API_KEY=sk-...

# .env
NEXT_PUBLIC_SUPABASE_URL=https://...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

### Dependencies:
- `openai` - GPT API
- `uuid` - генерация ID
- `pdf-parse` v1.1.1 - извлечение текста
- `pdf2json` - резервный метод

## 🎯 Результат

Стабильный, надежный парсер с:
- ✅ Защитой от потери данных
- ✅ Детальным логированием
- ✅ UI интеграцией
- ✅ Поддержкой билингвальных документов
- ✅ Автоматической генерацией тегов
- ✅ Валидацией данных

---

**Дата:** 2026-01-25  
**Статус:** ГОТОВ К ПРОИЗВОДСТВУ ✅
