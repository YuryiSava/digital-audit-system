# Настройка Supabase для Digital Audit System

## Шаг 1: Регистрация на Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите **Start your project**
3. Войдите через GitHub или создайте аккаунт

## Шаг 2: Создание проекта

1. Нажмите **New Project**
2. Выберите организацию (или создайте новую)
3. Заполните данные проекта:
   - **Name**: `digital-audit-system`
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, `Frankfurt` для Казахстана)
   - **Pricing Plan**: выберите **Free** (достаточно для разработки)
4. Нажмите **Create new project**

⏱️ Подождите 1-2 минуты, пока проект создается.

## Шаг 3: Получение Connection String

1. В левом меню выберите **Settings** (⚙️)
2. Перейдите в **Database**
3. Прокрутите вниз до раздела **Connection string**
4. Выберите вкладку **URI**
5. Скопируйте строку подключения (она выглядит так):

```
postgresql://postgres.xxxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

6. **Замените** `[YOUR-PASSWORD]` на пароль, который вы создали в Шаге 2

## Шаг 4: Получение API Keys

1. В левом меню выберите **Settings** (⚙️)
2. Перейдите в **API**
3. Найдите раздел **Project API keys**
4. Скопируйте:
   - **Project URL** (например: `https://xxxxxxxxxxxxx.supabase.co`)
   - **anon public** key (длинная строка)

## Шаг 5: Создание файла .env

1. В корне проекта `digital-audit-system` создайте файл `.env`
2. Вставьте следующее содержимое:

```env
# Database (замените на вашу строку подключения)
DATABASE_URL="postgresql://postgres.xxxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres"

# Supabase (замените на ваши данные)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-very-long-anon-key-here

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

3. **Замените**:
   - `[YOUR-PASSWORD]` на ваш пароль
   - `xxxxxxxxxxxxx` на ваш project ref
   - `your-very-long-anon-key-here` на ваш anon key

## Шаг 6: Инициализация базы данных

Откройте терминал в папке проекта и выполните:

```bash
# 1. Создать таблицы в базе данных
npm run db:push

# 2. Заполнить справочники (системы, типы дефектов и т.д.)
npm run db:seed
```

✅ Если всё прошло успешно, вы увидите:
```
✅ Database seed completed!
```

## Шаг 7: Проверка базы данных

### Вариант A: Через Prisma Studio (рекомендуется)

```bash
npm run db:studio
```

Откроется браузер с GUI для базы данных. Проверьте, что таблицы созданы и справочники заполнены.

### Вариант B: Через Supabase Dashboard

1. Перейдите в ваш проект на supabase.com
2. В левом меню выберите **Table Editor**
3. Вы должны увидеть все таблицы: `systems`, `defect_types`, `severity_levels` и т.д.

## Шаг 8: Запуск приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🎉 Готово!

Теперь у вас полностью настроенная система Digital Audit System.

---

## ⚠️ Troubleshooting

### Ошибка: "Can't reach database server"

**Проблема**: Неверная строка подключения или пароль.

**Решение**:
1. Проверьте, что вы заменили `[YOUR-PASSWORD]` на реальный пароль
2. Убедитесь, что нет лишних пробелов в `.env`
3. Попробуйте сбросить пароль базы данных в Supabase Dashboard

### Ошибка: "Prisma schema not found"

**Проблема**: Вы не в корневой папке проекта.

**Решение**:
```bash
cd C:\Users\1\.gemini\antigravity\scratch\digital-audit-system
```

### Ошибка при `npm run db:push`

**Проблема**: База данных не доступна.

**Решение**:
1. Проверьте интернет-соединение
2. Убедитесь, что проект Supabase активен (не приостановлен)
3. Проверьте правильность `DATABASE_URL` в `.env`

---

## 📚 Дополнительные ресурсы

- [Supabase Documentation](https://supabase.com/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [Next.js Documentation](https://nextjs.org/docs)
