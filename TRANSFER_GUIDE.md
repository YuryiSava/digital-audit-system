# 🚚 Руководство по переносу и развертыванию (v0.5)

Этот документ поможет вам перенести систему на другой сервер или компьютер и настроить доступ для мобильных устройств.

---

## 1. Перенос кода на новый компьютер

### Минимум для запуска:
1. **Node.js (v18.x или v20.x)** — загрузите с [nodejs.org](https://nodejs.org/).
2. **Git** (опционально, если используете репозиторий).

### Инструкция:
1. Скопируйте папку с проектом.
2. Проверьте наличие внутри файла `.env`.
   - Если его нет, создайте его и вставьте:
     ```env
     NEXT_PUBLIC_SUPABASE_URL=ваш_url
     NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
     OPENAI_API_KEY=ваш_ключ_ai
     ```
3. Откройте терминал (PowerShell / CMD) в этой папке.
4. Выполните установку зависимостей:
   ```bash
   npm install
   ```
5. Запустите в режиме разработки:
   ```bash
   npm run dev
   ```

---

## 2. Настройка доступа для мобильных устройств (Телефоны/Планшеты)

### Вариант 1: Через локальную сеть (Для презентаций в офисе)
1. Убедитесь, что телефон и компьютер в одном Wi-Fi.
2. На компьютере откройте командную строку и введите `ipconfig`.
3. Найдите строку `IPv4 Address` (например, `192.168.0.105`).
4. На телефоне откройте браузер и введите: `http://192.168.0.105:3000`.

### Вариант 2: Публикация в интернет (Для работы на объектах)
Рекомендуется использовать **Vercel** (стандарт для Next.js):
1. Зарегистрируйтесь на [vercel.com](https://vercel.com).
2. Установите Vercel CLI: `npm i -g vercel`.
3. В папке проекта введите: `vercel`.
4. Следуйте инструкциям (просто жмите Enter).
5. В панели управления Vercel на сайте добавьте ваши переменные из `.env` в разделе **Settings -> Environment Variables**.
6. Сделайте повторный деплой: `vercel --prod`.
7. **Результат:** У вас будет постоянная ссылка (https://...), доступная из любой точки мира.

---

## 3. Установка на смартфон (PWA)
Ваше приложение поддерживает установку без App Store:
- **iPhone (Safari):** Нажать кнопку "Поделиться" (квадрат со стрелкой) -> "На экран Домой".
- **Android (Chrome):** Нажать "Три точки" в углу -> "Установить приложение".

---

## 4. Перенос базы данных (Supabase)
Вам **НЕ НУЖНО** переносить базу данных. Она облачная. 
Как только вы запустите код на новом месте с тем же файлом `.env`, все ваши проекты, 1776 требований и фото-отчеты автоматически подгрузятся.

---
**Если возникли сложности с установкой Node.js или Vercel — пишите, я подскажу конкретные шаги!**
