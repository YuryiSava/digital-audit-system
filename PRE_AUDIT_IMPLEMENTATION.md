# Pre-Audit Setup - Implementation Summary

## ✅ Что реализовано (26.01.2026)

### 1. Расширение модели данных

**Добавлены поля в таблицу `projects`:**
- `systemsInScope` (TEXT[]) - массив ID систем для аудита
- `scopeDepth` (TEXT) - глубина аудита (BASIC, STANDARD, DEEP)
- `scopeExclusions` (TEXT) - исключения из scope
- `baselineFrozen` (BOOLEAN) - флаг заморозки baseline
- `baselineFrozenAt` (TIMESTAMP) - дата заморозки
- `baselineFrozenBy` (TEXT) - кто заморозил

**Миграция:** `migrations/add_project_preaudit_fields.sql`

### 2. Server Actions

**Файл:** `app/actions/project-preaudit.ts`

Функции:
- `updateProjectScope()` - обновление scope систем
- `freezeProjectBaseline()` - заморозка baseline и создание чек-листов
- `getAvailableRequirementSets()` - получение доступных наборов требований
- `getProjectPreAuditProgress()` - прогресс pre-audit setup

### 3. UI Компоненты

**Pre-Audit Wizard:**
- Файл: `components/projects/project-preaudit-wizard.tsx`
- Маршрут: `/projects/[id]/pre-audit`
- Страница: `app/projects/[id]/pre-audit/page.tsx`

**Функционал:**
- Шаг 1: Просмотр информации о проекте
- Шаг 2: Выбор систем для аудита (scope)
- Шаг 3: Выбор наборов требований
- Кнопка "Freeze Baseline" - создает чек-листы

**Интеграция:**
- Добавлена кнопка "Pre-Audit Setup" на странице проекта
- Показывает статус "Baseline Frozen" после заморозки

### 4. Как работает Freeze Baseline

При нажатии "Freeze Baseline":

1. **Создаются AuditChecklists** для каждого выбранного RequirementSet
2. **Создаются AuditResults** для всех требований в каждом наборе
3. **Обновляется Project:**
   - `baselineFrozen = true`
   - `baselineFrozenAt = текущая дата`
   - `status = IN_PROGRESS`

После заморозки:
- Нельзя изменить scope
- Нельзя добавить/удалить requirement sets
- Можно начинать полевые работы (заполнять чек-листы)

## 📋 Архитектурные решения

### Почему используем Project, а не Audit?

В schema.prisma есть две модели:
1. **Project** - упрощенная модель (используется сейчас)
2. **Audit** - полная модель (не используется)

Мы интегрировали Pre-Audit в существующую модель **Project**, чтобы:
- ✅ Не ломать существующий функционал
- ✅ Использовать уже работающие компоненты
- ✅ Минимизировать изменения

### Файлы для будущей миграции на Audit

Эти файлы созданы, но НЕ используются:
- `app/actions/baseline.ts` - для модели Audit
- `app/actions/pre-audit.ts` - для модели Audit
- `components/pre-audit/pre-audit-wizard.tsx` - для модели Audit

Они готовы для будущей миграции на полную модель Audit.

## 🚀 Как использовать

### 1. Создать проект
```
/projects → Создать проект
```

### 2. Настроить Pre-Audit
```
/projects/[id] → Pre-Audit Setup
```

### 3. Выбрать системы
- Выберите системы (APS, SOUE, и т.д.)
- Установите глубину аудита
- Сохраните

### 4. Выбрать наборы требований
- Выберите requirement sets для выбранных систем
- Нажмите "Freeze Baseline"

### 5. Начать аудит
- После заморозки появятся чек-листы
- Переходите в чек-листы и заполняйте результаты

## 📊 Статистика

**Создано файлов:** 6
- 1 SQL миграция
- 2 server actions файла
- 2 UI компонента
- 1 скрипт проверки миграции

**Добавлено полей в БД:** 6 (в таблицу projects)

**Строк кода:** ~800

## 🔄 Следующие шаги

### Приоритет 1: Тестирование
- [ ] Создать тестовый проект
- [ ] Пройти весь flow Pre-Audit Setup
- [ ] Проверить создание чек-листов
- [ ] Проверить заполнение чек-листов

### Приоритет 2: Улучшения UI
- [ ] Добавить индикатор прогресса
- [ ] Показывать количество требований в каждом наборе
- [ ] Добавить предпросмотр требований

### Приоритет 3: Функционал
- [ ] Добавить Test Plans (Шаг 4)
- [ ] Добавить Document Requests (Шаг 5)
- [ ] Добавить возможность unfreeze (для админов)

### Приоритет 4: Миграция на Audit
- [ ] Создать миграцию данных Project → Audit
- [ ] Переписать существующие страницы
- [ ] Использовать полную модель Audit

## 📝 Примечания

- Все новые поля в `projects` имеют значения по умолчанию
- Существующие проекты автоматически получат пустые значения
- Freeze Baseline - необратимая операция (пока нет unfreeze)
- Requirement Sets должны иметь status='PUBLISHED' чтобы появиться в списке

## 🐛 Известные ограничения

1. Нет валидации на уровне БД для `systemsInScope`
2. Нет проверки что все системы в scope имеют requirement sets
3. Нет возможности изменить scope после заморозки
4. Нет audit trail для изменений scope

## 📚 Связанные файлы

### Модели данных
- `prisma/schema.prisma` - Project, AuditChecklist, AuditResult

### Server Actions
- `app/actions/project-preaudit.ts` - основной функционал
- `app/actions/projects.ts` - CRUD проектов
- `app/actions/audit.ts` - работа с чек-листами

### UI
- `app/projects/[id]/page.tsx` - страница проекта
- `app/projects/[id]/pre-audit/page.tsx` - страница pre-audit
- `components/projects/project-preaudit-wizard.tsx` - wizard компонент

### Миграции
- `migrations/add_project_preaudit_fields.sql` - SQL миграция
- `scripts/apply-preaudit-migration.js` - скрипт проверки

---

**Дата:** 26.01.2026  
**Версия:** 1.0  
**Статус:** ✅ Реализовано и готово к тестированию
