# Offline Mobile: Анализ и План Реализации

## 🔍 Что Было Сделано (v0.6.0)

### ✅ Частично Реализовано
**Фаза 1 (2 февраля 2026) - Базовый offline:**
- ✅ Hook `use-offline-sync.ts` создан
- ✅ IndexedDB интеграция (Dexie.js)
- ✅ Сохранение статусов offline (compliant/non-compliant/n_a)
- ✅ Сохранение комментариев offline
- ✅ Индикатор online/offline в UI
- ✅ Автоматическая синхронизация при появлении сети

### ❌ НЕ Реализовано
**Фаза 2 - Фото offline:**
- ❌ Сохранение фото в IndexedDB как Blob
- ❌ Очередь загрузки фото
- ❌ Компрессия фото перед загрузкой

**Фаза 3 - Полнофункциональность:**
- ❌ Quantitative Data (счетчики нарушений) offline
- ❌ Разрешение конфликтов при синхронизации
- ❌ Manual sync button
- ❌ Sync history log

### 🚫 Почему Откатили?
1. **Фото не работали offline** - критично для инженеров
2. **Не тестировалось на реальных устройствах** - только на Chrome DevTools
3. **Quantitative Data терялись** - счетчики нарушений не сохранялись
4. **Конфликты данных** - при работе нескольких инженеров одновременно

---

## 💡 Можно ли Сделать? ДА!

### Техническая Осуществимость: ✅ 100%

**Почему это возможно:**
1. ✅ **Фундамент уже есть** - `use-offline-sync.ts` работает
2. ✅ **IndexedDB поддерживается** - все современные браузеры
3. ✅ **PWA-совместимость** - Next.js поддерживает Service Workers
4. ✅ **Supabase offline-ready** - можно использовать Realtime + Local DB

**Референсы успешных реализаций:**
- Firebase Offline Persistence (миллионы приложений)
- PouchDB + CouchDB (медицинские системы)
- Realm Database (полевые приложения)

---

## 🛠️ План Реализации v2 (Правильный Подход)

### Принцип: "Не Ломать Core"

> **Архитектура:** Offline - это **отдельный слой** поверх существующей системы, а не замена!

### Этап 1: Минимально Работающий Offline (MVP)
**Цель:** Инженер может заполнить чек-лист БЕЗ интернета, всё синхронизируется потом.

**Что делаем:**
1. ✅ Используем существующий `use-offline-sync.ts` (уже работает!)
2. ✅ Добавляем поддержку фото:
   ```typescript
   // IndexedDB Schema
   photos: {
     id: string,
     blob: Blob,           // Сжатое фото
     checkItemId: string,
     uploadStatus: 'pending' | 'uploading' | 'done',
     createdAt: timestamp
   }
   ```
3. ✅ Компрессия на клиенте (библиотека `browser-image-compression`):
   - Оригинал 5MB → Сжатый 500KB
   - Качество 80%, макс. 1920px
4. ✅ Очередь загрузки с повторами

**Результат:** Инженер работает offline, включает Wi-Fi → всё загружается автоматически.

**Сроки:** 1-2 недели разработки + 1 неделя тестирования.

---

### Этап 2: Продвинутый Offline (Production-Ready)
**Цель:** Устранить все edge cases и конфликты.

**Что добавляем:**
1. **Конфликт-резолюция:**
   ```typescript
   // Стратегия: "Server Wins" для критичных данных
   // "Client Wins" для комментариев и фото
   
   if (serverVersion > clientVersion) {
     // Показать диалог: "Данные изменены на сервере. Перезаписать?"
   }
   ```

2. **Quantitative Data offline:**
   - Счетчики `totalCount`, `failCount` сохраняются локально
   - Merge логика при синхронизации

3. **Manual Sync Control:**
   - Кнопка "Синхронизировать сейчас"
   - Индикатор pending items: "⏳ 15 изменений ждут загрузки"

4. **Battery Optimization:**
   - Sync только при Wi-Fi (опционально)
   - Suspend sync при низком заряде (<20%)

**Сроки:** 2-3 недели.

---

### Этап 3: PWA (Progressive Web App)
**Цель:** Работа как нативное приложение.

**Что добавляем:**
1. **Service Worker** - кеширование UI и статики
2. **Add to Home Screen** - иконка на экране телефона
3. **Background Sync** - загрузка даже когда приложение закрыто
4. **Push Notifications** - "Синхронизация завершена!"

**Сроки:** 1-2 недели (это просто конфигурация).

---

## ⚠️ Риски и Митигация

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Фото не загружаются | Средняя | Очередь с retry + индикатор прогресса |
| Конфликты данных | Высокая | Стратегия "Server Wins" + диалог выбора |
| Переполнение IndexedDB | Низкая | Лимит 50MB данных + очистка старых |
| Battery drain | Средняя | Sync только при зарядке/Wi-Fi |
| Ломается основная система | **Критическая** | **Отдельная ветка + Feature Flag** |

---

## 🎯 Рекомендуемый Подход

### Вариант А: "Безопасный Incremental"
**План:**
1. Создать **отдельную ветку** `feature/offline-v2`
2. Не трогать текущий production код
3. Использовать **Feature Flag**:
   ```typescript
   const OFFLINE_ENABLED = process.env.NEXT_PUBLIC_OFFLINE_MODE === 'true';
   
   if (OFFLINE_ENABLED) {
     // Offline логика
   } else {
     // Старая онлайн логика
   }
   ```
4. Тестировать на **staging** (Vercel preview)
5. Включить для **1-2 тестовых инженеров**
6. После 2 недель успешной работы → раскатка на всех

**Преимущества:**
- ✅ Не ломает текущую работу
- ✅ Можно откатить одной командой
- ✅ Постепенное внедрение

**Недостатки:**
- ⚠️ Дублирование кода (временно)
- ⚠️ Требует дисциплины (не мержить в main рано)

---

### Вариант Б: "Параллельное Приложение" (Radical)
**План:**
1. Создать **отдельное** мобильное приложение:
   - `das-field-offline` (React Native / Capacitor)
   - Полностью offline-first
   - Синхронизируется с основной DAS через API
2. Основная система остается онлайн
3. Инженеры выбирают: веб или мобильное

**Преимущества:**
- ✅ Полная изоляция рисков
- ✅ Нативные возможности (GPS, камера)
- ✅ Лучший UX для мобильных

**Недостатки:**
- ❌ Большая разработка (2-3 месяца)
- ❌ Поддержка двух кодовых баз
- ❌ Публикация в App Store / Google Play

---

## 🚦 Мое Рекомендация

### 📌 Выбрать: **Вариант А (Безопасный Incremental)**

**Порядок действий:**
1. **Неделя 1-2:** Доделать Фото offline в отдельной ветке
2. **Неделя 3:** Тестирование на staging + 2 инженера
3. **Неделя 4:** Исправить баги, добавить manual sync
4. **Неделя 5:** Раскатка на 50% инженеров
5. **Неделя 6:** Полная раскатка или откат (по результатам)

**Feature Flag конфигурация:**
```bash
# .env.local (для тестирования)
NEXT_PUBLIC_OFFLINE_MODE=true

# Production (по умолчанию выключено)
NEXT_PUBLIC_OFFLINE_MODE=false
```

**Критерии успеха:**
- ✅ 0 потерь данных за 2 недели тестирования
- ✅ Фото загружаются в течение 5 минут после подключения
- ✅ Инженеры могут работать в подвале/метро без интернета
- ✅ Sync не разряжает батарею (< 5% за день)

---

## 📋 Чек-лист "Готово к Production Offline"

- [ ] Базовый offline (статусы, комментарии) работает
- [ ] Фото сохраняются offline и загружаются online
- [ ] Quantitative Data работает offline
- [ ] Конфликты разрешаются автоматически или с участием пользователя
- [ ] Manual sync кнопка работает
- [ ] Индикатор "pending items" показывает верные цифры
- [ ] Протестировано на 3+ реальных Android/iOS устройствах
- [ ] Работает при плохом интернете (3G, прерывания)
- [ ] Не разряжает батарею
- [ ] Можно откатить одной командой (Feature Flag)

---

## 💬 Итог

**Да, можем сделать offline!** Но нужно:
1. ✅ **Не трогать рабочую систему** - Feature Flag
2. ✅ **Доделать фото** - это 80% проблемы
3. ✅ **Тестировать на реальных устройствах** - не в эмуляторе
4. ✅ **Постепенный rollout** - сначала 2 человека, потом все

**Сроки:** 4-6 недель от старта до полной раскатки.

**Риск:** Средний (если использовать Feature Flag и отдельную ветку).

Готовы начать?
