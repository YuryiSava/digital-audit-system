# 📊 Стратегия масштабирования базы данных DAS (Database Scalability)

Этот документ описывает план обеспечения высокой производительности данных при росте количества проектов и пользователей.

## 1. Текущий технологический стек
- **Primary Database**: PostgreSQL (через Supabase).
- **ORM**: Prisma (Server-side) + Supabase Client (RLS).
- **Local Cache**: Dexie.js (IndexedDB) для оффлайн-работы.
- **Media Storage**: Supabase Storage (S3-compatible).

## 2. Прогноз нагрузки и объемов
| Показатель | Пилот (Текущий) | Enterprise (1 год) | Критический порог |
| :--- | :--- | :--- | :--- |
| Активные проекты | 5-10 | 100-200 | > 500 |
| Требований в БД | ~50,000 | ~1,000,000+ | > 5,000,000 |
| Результаты аудита | ~10,000 | ~500,000 | > 2,000,000 |
| Фото/доказательства | < 1 ГБ | 50-100 ГБ | > 500 ГБ |

## 3. Критические риски и «Бутылочные горлышки»
1. **Эффект RLS (Row Level Security)**: При росте таблицы `audit_results` до миллионов строк, проверка прав доступа к каждой строке замедлит выборку.
2. **Лимиты подключений**: Прямое использование Prisma в Serverless-функциях может исчерпать доступные слоты PostgreSQL (Max Connections).
3. **Сложные реляции**: JOIN-запросы между `NormSource` -> `Requirement` -> `AuditResult` -> `Project` станут тяжелыми.

---

## 4. Стратегия предупреждения проблем

### А. Мониторинг «Раннего предупреждения»
- [ ] **Weekly Growth Audit**: Еженедельный замер размера БД. При достижении 70% лимита тарифа — автоматическое уведомление.
- [ ] **Query Performance Analytics**: Использование расширения `pg_stat_statements` для выявления запросов со временем выполнения > 200мс.

### Б. Архитектурные меры (Horizontal Scaling)
- [ ] **Партиционирование (Table Partitioning)**: Разделение таблицы `audit_results` на партиции по `ProjectID`. Поиск данных внутри одного проекта будет идти мгновенно, не затрагивая миллионы строк других проектов.
- [ ] **Денормизация данных (Read Models)**: Создание специальных таблиц-«снимков» для Dashboard, чтобы исключить сложные объединения при каждом входе пользователя.

### В. Очистка и архив (Lifecycle Management)
- [ ] **Cold Storage Strategy**: Перенос данных завершенных и подписанных проектов (старше 2 лет) в архивную схему или JSON-архивы в S3.
- [ ] **Image Optimization**: Автоматическое сжатие фотографий на клиенте/сервере перед загрузкой для экономии места.

---

## 5. План миграции (Migration Path)

### Уровень 1: Облачное масштабирование (Supabase Pro)
- Переход на выделенные инстансы БД с увеличенным RAM и CPU.
- Использование Pooler (pgBouncer) для управления соединениями.

### Уровень 2: Гибридный или Self-hosted
- При достижении объема > 1 ТБ или при жестких требованиях безопасности: перенос PostgreSQL на выделенный Linux-кластер.
- Разделение базы на «Операционную» (текущие аудиты) и «Аналитическую» (история и библиотека норм).

## 6. Рекомендации
1. **Индексация**: Начать использование GIN-индексов для полнотекстового поиска в нормах.
2. **Batching**: Все операции записи в полевых условиях должны идти пакетами (Batch Updates) для снижения количества транзакций.
