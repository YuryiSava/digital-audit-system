# BUG: Теги фрагментов — проблемы с сохранением и отображением

**Дата:** 2026-02-05
**Версия:** v0.7.48

---

## ✅ ИСПРАВЛЕНО: Ошибка сохранения тегов

**Статус:** 🟢 Закрыт
**Решение:** Выполнена миграция БД (2026-02-05 12:55)

```sql
ALTER TABLE raw_norm_fragments ADD COLUMN IF NOT EXISTS tags TEXT[] DEFAULT ARRAY[]::TEXT[];
ALTER TABLE raw_norm_fragments ADD COLUMN IF NOT EXISTS check_method TEXT;
```

---

## 🔴 ОТКРЫТО: UI не отображает выбранные теги

**Статус:** 🔴 Открыт
**Приоритет:** Средний

### Описание
При открытии фрагмента чекбоксы тегов не показывают ранее выбранные значения, хотя теги сохраняются в БД корректно.

### Причина (вероятно)
Компонент `RawFragmentsTab.tsx` не загружает существующие теги из поля `tags` при инициализации состояния чекбоксов.

### Файлы для исправления
- `components/normative/RawFragmentsTab.tsx` — добавить загрузку `fragment.tags` в начальное состояние
